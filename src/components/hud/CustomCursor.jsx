/**
 * CustomCursor is handled via 100% hardware-accelerated CSS SVG cursors in cinematic.css.
 * This guarantees literal 0ms latency, native 1000Hz USB polling responsiveness,
 * and zero DOM trailing lag across all browsers and devices.
 */
const CustomCursor = () => null;

export default CustomCursor;
